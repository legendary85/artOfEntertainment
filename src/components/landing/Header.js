import React from 'react';
import { Jumbotron, Container } from 'reactstrap';
import './Header.css'

const Header = () => {
  return (
    <section>
      <Jumbotron fluid>
        <Container fluid>
        </Container>
      </Jumbotron>

    </section>

  )
}

export default Header